# Scope boundary

This change only reorders canonical DiscourseState thread handling. It does not change semantic interpretation, Memory persistence, relationship state, ResponsePlan ownership, or speech realization. Existing ambiguity behavior for multiple open threads remains fail-closed.
