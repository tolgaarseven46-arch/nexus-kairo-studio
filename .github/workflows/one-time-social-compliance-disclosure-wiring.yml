name: One-time social compliance disclosure wiring

on:
  push:
    paths:
      - ".github/workflows/one-time-social-compliance-disclosure-wiring.yml"

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - name: Apply social compliance disclosure wiring fix
        run: node scripts/applySocialComplianceDisclosureWiringFix.mjs
      - name: Targeted contracts
        run: npx vitest run src/services/kairaSocialComplianceDisclosureWiringContracts.test.ts src/services/socialOrientationEngine.test.ts src/services/behaviorIntegrationEngine.test.ts
      - name: TypeScript
        run: npm run lint
      - name: Full regression
        run: npm test
      - name: Production build
        run: npm run build
      - name: Commit integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/services/behaviorIntegrationEngine.ts src/services/kairaSocialComplianceDisclosureWiringContracts.test.ts
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "fix(kaira): wire social compliance and disclosure downstream"
          git pull --rebase origin main
          git push origin main
