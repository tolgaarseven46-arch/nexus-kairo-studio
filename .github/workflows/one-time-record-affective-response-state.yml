name: One-time affective response state recorder

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/one-time-record-affective-response-state.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Record affective response state and clean helpers
        run: |
          node scripts/one-time-record-affective-response-state.mjs
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/one-time-record-affective-response-state.mjs
          git rm .github/workflows/one-time-record-affective-response-state.yml
          git add PROJECT_STATE.md
          git commit -m "docs(kaira): record affective response consistency"
          git push
