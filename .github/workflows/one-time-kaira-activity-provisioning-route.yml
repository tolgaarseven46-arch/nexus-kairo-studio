name: one-time-kaira-activity-provisioning-route
on:
  push:
    paths:
      - ".github/workflows/one-time-kaira-activity-provisioning-route.yml"
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/patch-kaira-activity-provisioning-route.mjs
      - name: Commit activity provisioning route wiring
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server.ts
          git diff --cached --quiet && exit 1
          git commit -m "feat(kaira): register activity provisioning routes"
          git push
