name: One-time provider observability localize

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply provider observability patch
        run: python scripts/one-time-provider-observability-localize.py
      - name: Commit patch and cleanup
        run: |
          rm scripts/one-time-provider-observability-localize.py
          rm .github/workflows/one-time-provider-observability-localize.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server.ts src/services/droitChatService.ts scripts/one-time-provider-observability-localize.py .github/workflows/one-time-provider-observability-localize.yml
          git commit -m "fix(kaira): make provider observability request-local"
          git push
