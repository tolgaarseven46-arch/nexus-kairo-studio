name: One-time preference overstimulation wiring

on:
  push:
    paths:
      - ".github/workflows/one-time-preference-overstimulation-wiring.yml"

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - name: Apply preference overstimulation wiring fix
        run: node scripts/applyPreferenceOverstimulationWiringFix.mjs
      - name: Targeted contracts
        run: npx vitest run src/services/kairaPreferenceOverstimulationWiringContracts.test.ts src/services/preferenceEngine.test.ts src/services/behaviorIntegrationEngine.test.ts
      - name: TypeScript
        run: npm run lint
      - name: Full regression
        run: npm test
      - name: Production build
        run: npm run build
      - name: Commit integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/services/behaviorIntegrationEngine.ts src/services/kairaPreferenceOverstimulationWiringContracts.test.ts
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "fix(kaira): wire preference overstimulation downstream"
          git pull --rebase origin main
          git push origin main
