name: One-time Live Quality Smoke

on:
  push:
    branches:
      - codex/one-time-live-quality-smoke

jobs:
  live-quality-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Exercise deployed Kaira with cheap Gemini
        env:
          KAIRA_URL: https://nexus-kairo-studio.onrender.com/api/chat
        run: |
          python - <<'PY'
          import json, os, time, urllib.request, urllib.error

          url = os.environ["KAIRA_URL"]
          user_id = "live_quality_smoke_20260915"
          session_id = f"session_{user_id}_{int(time.time())}"
          history = []
          messages = [
              "kanka bugün müdürle fena tartıştım",
              "yarın istifa etmeyi düşünüyorum ama daha karar vermedim",
              "sen olsan ne yapardın",
              "neyse boşver, biraz sakinleşmek istiyorum",
          ]

          def post(payload):
              data = json.dumps(payload, ensure_ascii=False).encode("utf-8")
              req = urllib.request.Request(
                  url,
                  data=data,
                  headers={"Content-Type": "application/json"},
                  method="POST",
              )
              try:
                  with urllib.request.urlopen(req, timeout=60) as response:
                      return response.status, json.loads(response.read().decode("utf-8"))
              except urllib.error.HTTPError as exc:
                  body = exc.read().decode("utf-8", errors="replace")
                  print(f"HTTP_ERROR {exc.code}: {body}")
                  raise

          for index, message in enumerate(messages, 1):
              payload = {
                  "userId": user_id,
                  "userName": "Tolga",
                  "userMessage": message,
                  "history": history,
                  "provider": "gemini",
                  "sessionId": session_id,
                  "requestId": f"live_quality_{int(time.time())}_{index}",
                  "suppressRecentMemory": True,
              }
              status, result = post(payload)
              reply = str(result.get("reply") or "").strip()
              provider = result.get("providerUsed")
              print(f"TURN {index}")
              print(f"USER: {message}")
              print(f"PROVIDER: {provider}")
              print(f"KAIRA: {reply}")
              print(f"CONSISTENCY_ACCEPTED: {result.get('consistency', {}).get('accepted')}")
              print(f"RESPONSE_PLAN: {json.dumps(result.get('metadata', {}).get('responsePlan'), ensure_ascii=False)}")
              print("---")
              if status != 200 or not reply:
                  raise SystemExit(f"turn {index} failed: status={status}, reply={reply!r}")
              history.append({"sender": "user", "text": message, "participantName": "Tolga"})
              history.append({"sender": "kaira", "text": reply})

          print("LIVE_QUALITY_SMOKE_COMPLETE")
          PY
