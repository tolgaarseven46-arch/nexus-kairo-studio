name: One-time provider repair learning

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python scripts/one-time-provider-repair-learning.py
      - name: Commit patch and cleanup
        run: |
          rm scripts/one-time-provider-repair-learning.py
          rm .github/workflows/one-time-provider-repair-learning.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server.ts scripts/one-time-provider-repair-learning.py .github/workflows/one-time-provider-repair-learning.yml
          git commit -m "fix(kaira): learn accepted provider repair after fallback"
          git push
