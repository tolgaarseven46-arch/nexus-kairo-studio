name: one-time-distributed-idempotency-wiring
on:
  push:
    paths:
      - '.github/workflows/one-time-distributed-idempotency-wiring.yml'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - run: python scripts/one-time-distributed-idempotency-wiring.py
      - run: |
          rm -f scripts/one-time-distributed-idempotency-wiring.py
          rm -f .github/workflows/one-time-distributed-idempotency-wiring.yml
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'feat(kaira): wire distributed chat idempotency'
          git push origin HEAD:main
