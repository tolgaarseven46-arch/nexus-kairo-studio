name: one-time-relationship-conditioned-appraisal
on:
  push:
    paths:
      - '.github/workflows/one-time-relationship-conditioned-appraisal.yml'
permissions:
  contents: write
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --ignore-scripts
      - name: Apply relationship-conditioned appraisal migration
        run: node scripts/one-time-relationship-conditioned-appraisal.cjs
      - name: Focused relationship appraisal regressions
        run: npx vitest run src/services/kairaRelationshipConditionedAppraisalContracts.test.ts src/services/kairaRelationshipQualitativeReactionCharacterization.test.ts src/services/kairaRelationshipHowDifferentiation.test.ts src/services/kdmConsistencyEngine.test.ts src/services/kairaQualitativeStateTransitions.test.ts
      - name: Architecture contracts
        run: npm run test:contracts
      - name: Full tests
        run: npm test
      - name: TypeScript
        run: npm run lint
      - name: Production build
        run: npm run build
      - name: Commit product and clean helper
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/one-time-relationship-conditioned-appraisal.yml scripts/one-time-relationship-conditioned-appraisal.cjs
          git add src/services/relationshipConditionedAppraisal.ts src/services/kairaRelationshipConditionedAppraisalContracts.test.ts src/services/kdmConsistencyEngine.ts
          git commit -m "refactor(kaira): add relationship-conditioned appraisal"
          git push
