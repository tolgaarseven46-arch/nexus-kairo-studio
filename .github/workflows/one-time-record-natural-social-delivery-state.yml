name: One-time natural social delivery state recorder

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/one-time-record-natural-social-delivery-state.yml'

permissions:
  contents: write

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Record state and clean helpers
        run: |
          node scripts/record-natural-social-delivery-state.mjs
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/record-natural-social-delivery-state.mjs
          git rm .github/workflows/one-time-record-natural-social-delivery-state.yml
          git add PROJECT_STATE.md
          git commit -m "docs(kaira): record natural social delivery baseline"
          git push
