name: One-time preference downstream coverage
# rerun-after-complexity-context-fix

on:
  push:
    paths:
      - ".github/workflows/one-time-preference-downstream-coverage.yml"

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - name: Add preference downstream coverage contracts
        run: node scripts/addPreferenceDownstreamCoverageContracts.mjs
      - name: Targeted contracts
        run: npx vitest run src/services/kairaPreferenceDownstreamCoverageContracts.test.ts src/services/preferenceEngine.test.ts src/services/kairaPreferenceOverstimulationWiringContracts.test.ts
      - name: TypeScript
        run: npm run lint
      - name: Full regression
        run: npm test
      - name: Production build
        run: npm run build
      - name: Commit contracts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/services/kairaPreferenceDownstreamCoverageContracts.test.ts
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "test(kaira): lock preference downstream coverage"
          git pull --rebase origin main
          git push origin main
